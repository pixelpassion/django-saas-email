============
Installation
============

At the command line::

    $ easy_install django-saas-email

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-saas-email
    $ pip install django-saas-email
